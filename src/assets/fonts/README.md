Place the font file `Quicksand-VariableFont_wght.ttf` here.

Path expected: src/assets/fonts/Quicksand-VariableFont_wght.ttf

If you have the TTF file, copy it into this folder so the `@font-face` in `src/style.css` can load it.
